<Window x:Class="Esp32.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Esp32"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File" Margin="2">
                <MenuItem x:Name="MniFileExit" Header="Exit" Click="MniFileExit_Click"/>
            </MenuItem>
        </Menu>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <!-- Plot -->
                <ColumnDefinition Width="Auto"/>
                <!-- Đường kẻ dọc -->
                <ColumnDefinition Width="215"/>
                <!-- Panel -->
            </Grid.ColumnDefinitions>

            <!-- BÊN TRÁI: Đồ thị -->
            <oxy:PlotView Grid.Column="0"
                      x:Name="EspPlotView"
                      Model="{Binding MyModel}"
                      Margin="10" />

            <!-- ĐƯỜNG KẺ DỌC (thay cho Separator) -->
            <Border Grid.Column="2" Margin="0 25 10 0" BorderBrush="Gray" BorderThickness="1" CornerRadius="3" VerticalAlignment="Top">
                <StackPanel Margin="10">
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Cổng COM:" FontWeight="Bold" Width="70" VerticalAlignment="Center"/>
                        <ComboBox x:Name="COMComboBox" Margin="0,5,0,5" Width="110"/>
                    </StackPanel>
                    <Button x:Name="BtnStart" Content="Start" Width="180" Height="30" HorizontalAlignment="Left" Click="StartButton_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>
